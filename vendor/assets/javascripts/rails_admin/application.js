// rails_admin assets are served via webpacker (asset_source: :webpacker)
// This stub prevents Sprockets from trying to compile rails_admin's own manifest,
// which references files only available in the webpacker source tree.
