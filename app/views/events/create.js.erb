<% if @event.proposal_id.present?
 %> $('#wait_date_header_container').html('
<%= ::Temple::Utils.escape_html((j render(partial: 'proposals/wait_date_header', locals: {proposal: @proposal}))) %>
'); 
<% else
 %> $('#calendar').fullCalendar('refetchEvents'); 
<% end %> $('#create_event_dialog').foundation('reveal', 'close'); 
