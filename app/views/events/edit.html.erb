<div class="mb-6 flex flex-col gap-3 border-b-2 border-base-content/15 pb-5 sm:flex-row sm:items-end sm:justify-between">
  <div>
    <h1 class="text-3xl font-extrabold leading-tight"><%= t('pages.events.edit.heading') %></h1>
    <p class="mt-2 max-w-2xl text-base-content/70"><%= t('pages.events.edit.subtitle', title: @event.title) %></p>
  </div>
</div>
<%= render 'form' %>
