| var container_ = $('
#area_role_container.reveal-modal[data-reveal]
| '); container_.html("
=j render(partial: 'area_roles/form', locals: {button_name: t('buttons.create'), remote: true})
| "); $('body').append(container_); airesis_reveal(container_); new AiresisFormValidation(container_.find('form')); 