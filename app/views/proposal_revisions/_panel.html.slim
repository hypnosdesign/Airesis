#proposal_histories.reveal-modal[data-reveal]
  h2
    = t('pages.proposal_histories.title')
  .row.full-width
    .columns.large-4
      = render partial: 'panel_menu'
    .columns.large-8
      = render partial: 'panel_content'
  a.close-reveal-modal
    | ×