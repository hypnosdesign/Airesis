<% @page_title = t('admin_ui.newsletters.edit_title', subject: @newsletter.subject) %>
<div class="max-w-4xl">
  <h1 class="mb-2 break-words text-3xl font-bold tracking-tight"><%= t('admin_ui.newsletters.edit_title', subject: @newsletter.subject) %></h1>
  <p class="mb-6 text-base-content/70"><%= t('admin_ui.newsletters.editor_description') %></p>
  <%= render 'form' %>
</div>
