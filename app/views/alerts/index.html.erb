<div class="card">
<h1><%= ::Temple::Utils.escape_html((t('pages.alerts.index.new_not_read'))) %>
</h1><ul>
<% if @new_alerts.any?
@new_alerts.each do |alert|
 %><li>
<% _slim_controls1 = link_to calculate_alert_path(alert) do %><% _slim_controls2 = ''.dup; 
; _slim_controls2 << ((::Temple::Utils.escape_html((alert.created_at.strftime("%d/%m/%Y %H:%M")))).to_s); 
; _slim_controls2 << ("- ".freeze); _slim_controls2 << ((::Temple::Utils.escape_html((raw alert.message))).to_s); 
; _slim_controls2 %><% end %><%= ::Temple::Utils.escape_html((_slim_controls1)) %></li><% end; else
 %><b><%= ::Temple::Utils.escape_html((t('pages.alerts.index.no_new'))) %>
</b><% end %></ul><h1><%= ::Temple::Utils.escape_html((t('pages.alerts.index.old_not_read'))) %>
</h1><ul>
<% if @old_alerts.any?
@old_alerts.each do |alert|
 %><li>
<% _slim_controls3 = link_to calculate_alert_path(alert) do %><% _slim_controls4 = ''.dup; 
; _slim_controls4 << ((::Temple::Utils.escape_html((alert.created_at.strftime("%d/%m/%Y %H:%M")))).to_s); 
; _slim_controls4 << ("- ".freeze); _slim_controls4 << ((::Temple::Utils.escape_html((raw alert.message))).to_s); 
; _slim_controls4 %><% end %><%= ::Temple::Utils.escape_html((_slim_controls3)) %></li><% end; else
 %><b><%= ::Temple::Utils.escape_html((t('pages.alerts.index.no_old'))) %>

</b><% end %></ul></div><% content_for :left_panel do
if current_user
 %><%= ::Temple::Utils.escape_html((render partial: 'users/preferences_panel')) %>

<% end; end %><script>
$.ajax({

  data: '',

  dataType: 'script',

  type: 'POST',

  url: "/alerts/check_all"

});</script>
