<header class="mb-6 max-w-2xl">
  <h1 class="mb-2 text-2xl font-extrabold lg:text-3xl"><%= t('pages.groups.invite_your_friends.title') %></h1>
  <p class="text-sm text-base-content/65"><%= @group.name %></p>
</header>

<div id="group_invitation_form" class="max-w-2xl border-2 border-base-content/15 bg-base-100 p-4 sm:p-6">
  <%= render 'group_invitations/form', modal: false %>
</div>
