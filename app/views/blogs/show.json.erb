{ "title": "
<%= ::Temple::Utils.escape_html((@blog.title)) %>
", "posts":
<%= ::Temple::Utils.escape_html((raw @blog_posts.to_json({only: [:id, :title, :created_at, :updated_at, :body], include: [user: {only: [:id], methods: [:fullname]}]}))) %>
 }
