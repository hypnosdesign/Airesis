<% @page_title = t('pages.blog_posts.edit.title') %>
<div class="card bg-base-200 p-4">
  <h2 class="text-xl font-bold mb-4"><%= t('pages.blog_posts.edit.title') %></h2>
  <%= render 'form' %>
</div>
