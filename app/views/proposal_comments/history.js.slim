| var container_ = $('
#proposal_comment_history.reveal-modal.small[data-reveal]
  | '); container_.append($('
  = j (render(partial: 'history'))
  | ')); $('body').append(container_); container_.foundation().foundation('reveal', 'open'); container_.css('max-height', $('html').height() - 110 + 'px').css('overflow-y', 'auto'); $(document).on('opened.fndtn.reveal', '[data-reveal]', function () { $('html').css('overflow', 'hidden'); }); $(document).on('closed.fndtn.reveal', '[data-reveal]', function () { $('html').css('overflow', ''); container_.remove(); }); 