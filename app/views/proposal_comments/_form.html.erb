<% _slim_controls1 = form_for [@proposal,@proposal_comment], remote: true do |f| %><% _slim_controls2 = ''.dup; 
; _slim_controls2 << ((::Temple::Utils.escape_html((f.text_area :content, class: 'edit_comment'))).to_s); 
; _slim_controls2 << ((::Temple::Utils.escape_html((link_to t('buttons.cancel'), '#', class: 'btn', data: {cancel_edit_comment: @proposal_comment.id}))).to_s); 
; _slim_controls2 << ((::Temple::Utils.escape_html((f.button t('buttons.update'), class: 'btn blue', data:{disable_with: t('updating')}))).to_s); 
; _slim_controls2 %><% end %><%= ::Temple::Utils.escape_html((_slim_controls1)) %>
