<%= link_to t('proposals.show.keep_discuss'), { action: 'rankdown', controller: 'proposals', id: @proposal.id, group_id: @group.try(:id) }, id: 'votedown', class: 'btn votedown', data: { disable_with: t('loading') } %>
