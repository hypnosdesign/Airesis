<%= link_to t('proposals.show.ready_for_vote'), { action: 'rankup', controller: 'proposals', id: @proposal.id, group_id: @group.try(:id) }, id: 'voteup', class: 'btn btn-primary voteup', data: { disable_with: t('loading') } %>
