ul.breadcrumbs
  li
    = link_to t('pages.groups.administration_panel.permissions'), group_participation_roles_path(@group)
  li.current
    = @page_title
h2
  = @page_title
.card
  = render partial: 'participation_roles/form', locals: {button_name: t('buttons.create')}
= content_for :left_panel do
  = render partial: 'groups/administration_panel'