<%= render layout: 'layouts/page_title', locals: { title: @page_title } do %>
  <%= link_to t('buttons.go_back'), group_quorums_path(@group), class: 'btn btn-outline min-h-11' %>
<% end %>

<div class="border-2 border-base-content/15 bg-base-100 p-4 sm:p-6">
  <%= render 'quorums/form', button_name: t('buttons.update'), modal: false %>
</div>

<% content_for :left_panel do %>
  <%= render 'groups/administration_panel' %>
<% end %>
