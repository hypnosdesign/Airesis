<%# Pagy nav — DaisyUI join component styling %>
<nav class="flex justify-center my-4" aria-label="pagination">
  <div class="join">
    <% if pagy.previous %>
      <%= link_to raw(t('views.pagination.previous')), pagy.page_url(:previous),
                  class: 'join-item btn btn-sm', rel: 'prev' %>
    <% end %>
    <% pagy.series.each do |item| %>
      <% if item.is_a?(Integer) %>
        <%= link_to item, pagy.page_url(item),
                    class: "join-item btn btn-sm #{item == pagy.page ? 'btn-active' : ''}" %>
      <% elsif item == :gap %>
        <span class="join-item btn btn-sm btn-disabled">…</span>
      <% end %>
    <% end %>
    <% if pagy.next %>
      <%= link_to raw(t('views.pagination.next')), pagy.page_url(:next),
                  class: 'join-item btn btn-sm', rel: 'next' %>
    <% end %>
  </div>
</nav>
