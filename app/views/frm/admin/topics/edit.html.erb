<div class="max-w-3xl">
  <h1 class="mb-6 text-3xl font-bold tracking-tight"><%= t('frm.topic.edit', subject: @topic.subject) %></h1>
  <div class="rounded-box border border-base-300 bg-base-100 p-4 sm:p-6"><%= render 'form' %></div>
</div>
