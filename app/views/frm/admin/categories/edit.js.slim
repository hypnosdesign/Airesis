| var container_ = $('
#category_container.reveal-modal[data-reveal]
| '); container_.html('
=j render partial: 'edit', locals: {remote: true}
| '); $('body').append(container_); airesis_reveal(container_); new AiresisFormValidation(container_.find('form')); 