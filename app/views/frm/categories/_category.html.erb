<% if can?(:read, category) %>
  <div id="category_<%= category.id %>" class="category mb-4">
    <h2 class="text-xl font-bold mb-2"><%= link_to category.name, group_frm_category_url(@group, category) %></h2>
    <%= render "/frm/forums/listing", category: category %>
  </div>
<% end %>
