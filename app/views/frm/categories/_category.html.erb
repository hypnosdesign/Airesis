<% if can?(:read, category)
 %><div id="category_<%= ::Temple::Utils.escape_html((category.id)) %>">.category
</div>
<h2>
<%= ::Temple::Utils.escape_html((link_to category.name, group_frm_category_url(@group,category))) %>
</h2><%= ::Temple::Utils.escape_html((render "/frm/forums/listing", category: category)) %>
<% end %>
