<nav class="breadcrumbs mb-6 text-sm" aria-label="<%= t('frm.navigation.breadcrumbs') %>">
  <ul>
    <li><%= link_to t('frm.forum.forums'), group_forums_url(@group), class: "inline-flex min-h-11 items-center" %></li>
    <li><%= link_to forum.category, group_frm_category_url(@group, forum.category), class: "inline-flex min-h-11 items-center" %></li>
    <li><%= link_to forum.title, group_forum_url(@group, forum), class: "inline-flex min-h-11 items-center", aria: { current: current_page?(group_forum_url(@group, forum)) ? "page" : nil } %></li>
  </ul>
</nav>
