| var container_ = $('
#group_area_container.reveal-modal[data-reveal]
| '); container_.html("
=j render(partial: 'group_areas/form', locals: {remote: true})
| "); $('body').append(container_); airesis_reveal(container_); new AiresisFormValidation(container_.find('form')); 